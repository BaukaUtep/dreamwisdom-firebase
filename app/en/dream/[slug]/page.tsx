import fs from 'node:fs';import path from 'node:path';const locale='en' as const;function readData(){const p=path.join(process.cwd(),'data',`dreams_${locale}.json`);const raw=fs.readFileSync(p,'utf-8');return JSON.parse(raw)}export async function generateStaticParams(){const data=readData();return data.map((e:any)=>({slug:e.slug}))}export default function DreamPage({params}:{params:{slug:string}}){const data=readData();const item=data.find((x:any)=>x.slug===params.slug);if(!item)return <div className='py-16'>Not found</div>;const jsonLd={'@context':'https://schema.org','@type':'CreativeWork','name':item.label,'about':'Islamic dream interpretation','inLanguage':locale,'alternateName':params.slug,'text':item.meaning};return(<article className='prose prose-slate max-w-none'><script type='application/ld+json' dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><h1>{item.label}</h1>{item.category&&<p><span className='text-gray-500'>Category:</span> {item.category}</p>}<div className='whitespace-pre-wrap leading-7'>{item.meaning}</div></article>)}